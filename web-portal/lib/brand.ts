/** Logo in `public/logo` (stable filename). */
export const LOGO_PATH = "/logo/logoclose.png" as const;

/** Core colours sampled from the logo — keep `app/globals.css` in sync. */
export const BRAND = {
  navy: "#1a427f",
  pink: "#f5b7c1",
} as const;
